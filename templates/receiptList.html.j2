<!DOCTYPE html>
<html lang="fr">
<head>
    <title>{{ _("Tous les reçus imprimés")}}</title>
</head>
<body style="display: flex;flex-direction: column;min-height: 100vh;">
<div id="page" style="flex-grow: 1;">
    {%if receiptlist|length > 0 %}
    <h1>{{receiptlist|length}} {%if receiptlist|length > 1%}{{_("recus")}}{%else%}{{_("recu")}}{%endif%} {%if receiptlist|length > 1%}{{_("disponibles")}}{%else%}{{_("disponible")}}{%endif%}</h1>
        <ul id="receiptlist">
            {%for receipt in receiptlist%}
                <li><a href="/recus/{{receipt[0]}}">{{receipt[1]}}</a></li>
            {%endfor%}
        </ul>
    {% else %}
        <h1>{{ _("Aucun reçu trouvé!")}}</h1>
    {% endif %}
    {# a comment #}
</div>
{% include 'footer.html.j2' %}
</body>
</html>